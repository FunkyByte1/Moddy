lib-cov
*.seed
*.log
*.csv
*.dat
*.out
*.pid
*.gz
*.swp

pids
logs
results

# Coverage reports
coverage

# API keys and secrets
.env

# Dependency directory
node_modules
bower_components
.pnpm-store

# Editors
.idea
*.iml

# OS metadata
.DS_Store
Thumbs.db

# Ignore built ts files
dist/

__pycache__/

/.yalc
yalc.lock

.vscode/settings.json

# Ignore output folder

backend/out

# Make sure to ignore any instance of the loader's decky_plugin.py
decky_plugin.py

# Ignore decky CLI for building plugins
out
out/*
cli/
cli/*
cli/decky

# Local build configuration (contains machine-specific paths)
Directory.Build.props

# Python cache
*.pyc
**/__pycache__/

# Decky plugin settings/runtime data (generated at runtime)
*.deckhand_bak
deploy.sh
releases/

# Sandbox/tooling artifacts (home dotfiles masked into the workspace, agent state)
.bash_profile
.bashrc
.gitconfig
.gitmodules
.mcp.json
.profile
.ripgreprc
.zprofile
.zshrc
.claude/

# Claude instructions — keep local, not public
CLAUDE.md
CLAUDE.local.md

# Local-only planning docs (never pushed to remote)
*.local.md

# Local scratch space for test artifacts (keep the dir, ignore its contents)
tmp/*
!tmp/.gitkeep
.vscode/defsettings.json
